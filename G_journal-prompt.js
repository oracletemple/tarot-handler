// G_journal-prompt.js - v1.0.0

/**
 * 返回灵魂日记引导句（用于写作反思）
 */
function getJournalPrompt() {
  return `📝 *Reflection Prompt*\n\nWhat part of me am I being called to surrender today, and what truth might emerge in that silence?`;
}

module.exports = { getJournalPrompt };
